const { StyleSheet } = require("react-native");

const styles = StyleSheet.create({
    image: {
        height: 400,
        width: '100%',
        backgroundColor: 'red'
    },
    container: {
        flex: 1,
        padding: 16,
        alignItems: 'center',
    }
})

export default styles;